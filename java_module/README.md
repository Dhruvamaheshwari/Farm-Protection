# 🌾 Crop Advisory System (Java Module)

This is a standalone command-line utility for the Kisan Seva project. It provides crop recommendations based on soil type and season.

## 📋 Prerequisites

*   **Java Development Kit (JDK)**: Ensure Java is installed on your system.
    *   Verify by running: `java -version`

## 🚀 How to Run

1.  **Open Terminal**: Navigate to this directory (`java_module`).
2.  **Compile**:
    ```bash
    javac CropAdvisory.java
    ```
3.  **Run**:
    ```bash
    java CropAdvisory
    ```

## 📝 Usage

Follow the on-screen prompts:
1.  Enter **Soil Type** (e.g., Black, Alluvial, Red).
2.  Enter **Season** (e.g., Kharif, Rabi).
3.  View the recommended crops.
